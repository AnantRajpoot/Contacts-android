<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Contacts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <color name="primaryTextColor">@color/white</color>

    <color name="searchViewBackground">@color/dark_gray</color>
    <color name="searchViewTextColor">@color/white</color>
    <color name="searchViewHintColor">@color/medium_gray</color>
    <color name="searchViewIconTint">@color/white</color>

    <color name="appBackgroundColor">@color/black</color>
    <color name="cardBackgroundColor">#1C1C1E</color>
    <color name="headerTextColor">#AEAEB2</color>
    <color name="iconBackgroundColor">#3A4078</color>
    <color name="iconTextColor">@color/white</color>

</resources>